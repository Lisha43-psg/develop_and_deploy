name: demoworkflow/demo

on:
  pull_request:
    branches:
      - main

jobs:
  demo-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run demo
        run: echo "All checks have passed!"
